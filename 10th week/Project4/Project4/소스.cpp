#include <stdio.h>
struct craker // 구조체 선언
{
	int price;// 가격을 저장할 멤버
	int calories;//열량을 저장할 멤버
};
int main()
{
	struct cracker basasak;
	printf("바사삭의 가격과 열량을 입력하세요 : ");
	scanf_s("%d%d", basasak.calories));
}